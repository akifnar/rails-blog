<!DOCTYPE html>
<html>

  <%= render "layouts/head" %>

  <body class="bg-gray-50 text-gray-900 pt-[60px]">
    <%= render "layouts/header" %>
    <main class="max-w-6xl mx-auto px-4 pt-24 pb-12">
      <%= yield %>
      <%= render "layouts/footer" %>
      <%= debug(params) if Rails.env.development? %>
    </main>
  </body>

</html>
